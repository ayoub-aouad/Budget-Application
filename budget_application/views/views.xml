<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="budget_application.list">
      <field name="name">BudgetApplication list</field>
      <field name="model">budget_application.budget_application</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="budget_application.action_window">
      <field name="name">BudgetApplication window</field>
      <field name="res_model">budget_application.budget_application</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="budget_application.action_server">
      <field name="name">BudgetApplication server</field>
      <field name="model_id" ref="model_budget_application_budget_application"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="BudgetApplication" id="budget_application.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="budget_application.menu_1" parent="budget_application.menu_root"/>
    <menuitem name="Menu 2" id="budget_application.menu_2" parent="budget_application.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="budget_application.menu_1_list" parent="budget_application.menu_1"
              action="budget_application.action_window"/>
    <menuitem name="Server to list" id="budget_application" parent="budget_application.menu_2"
              action="budget_application.action_server"/>
    -->
  </data>
</odoo>